{% comment %}
  Minimal test section - if this appears, your setup is working
{% endcomment %}

<div style="padding: 20px; border: 2px solid #333; margin: 20px 0; text-align: center;">
  <h2>{{ section.settings.title }}</h2>
  <p>{{ section.settings.description }}</p>
  <div style="background: #f0f0f0; padding: 20px; margin: 10px 0;">
    <p><strong>TEST:</strong> Section is working!</p>
    <iframe 
      src="https://craftons-curves-calculator.vercel.app"
      style="width: 100%; height: 400px; border: 1px solid #ccc;"
      title="Curves Calculator Test">
    </iframe>
  </div>
</div>

{% schema %}
{
  "name": "Curves Calculator Test",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Curves Calculator Test"
    },
    {
      "type": "text",
      "id": "description", 
      "label": "Description",
      "default": "This is a test section to verify setup is working."
    }
  ],
  "presets": [
    {
      "name": "Curves Calculator Test"
    }
  ]
}
{% endschema %} 